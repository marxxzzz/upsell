{
  "version": 2,
  "builds": [
    { "src": "api/*.mjs", "use": "@vercel/node" },
    { "src": "server.mjs", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/server.mjs" }
  ]
}
